#if GL_NV_PATH_RENDERING

public enum PathParameter : GLenum { 
#if GL_NV_PATH_RENDERING
    StrokeWidthNv = gl.Constants.GL_PATH_STROKE_WIDTH_NV, 
#endif

#if GL_NV_PATH_RENDERING
    EndCapsNv = gl.Constants.GL_PATH_END_CAPS_NV, 
#endif

#if GL_NV_PATH_RENDERING
    InitialEndCapNv = gl.Constants.GL_PATH_INITIAL_END_CAP_NV, 
#endif

#if GL_NV_PATH_RENDERING
    TerminalEndCapNv = gl.Constants.GL_PATH_TERMINAL_END_CAP_NV, 
#endif

#if GL_NV_PATH_RENDERING
    JoinStyleNv = gl.Constants.GL_PATH_JOIN_STYLE_NV, 
#endif

#if GL_NV_PATH_RENDERING
    MiterLimitNv = gl.Constants.GL_PATH_MITER_LIMIT_NV, 
#endif

#if GL_NV_PATH_RENDERING
    DashCapsNv = gl.Constants.GL_PATH_DASH_CAPS_NV, 
#endif

#if GL_NV_PATH_RENDERING
    InitialDashCapNv = gl.Constants.GL_PATH_INITIAL_DASH_CAP_NV, 
#endif

#if GL_NV_PATH_RENDERING
    TerminalDashCapNv = gl.Constants.GL_PATH_TERMINAL_DASH_CAP_NV, 
#endif

#if GL_NV_PATH_RENDERING
    DashOffsetNv = gl.Constants.GL_PATH_DASH_OFFSET_NV, 
#endif

#if GL_NV_PATH_RENDERING
    ClientLengthNv = gl.Constants.GL_PATH_CLIENT_LENGTH_NV, 
#endif

#if GL_NV_PATH_RENDERING
    FillModeNv = gl.Constants.GL_PATH_FILL_MODE_NV, 
#endif

#if GL_NV_PATH_RENDERING
    FillMaskNv = gl.Constants.GL_PATH_FILL_MASK_NV, 
#endif

#if GL_NV_PATH_RENDERING
    FillCoverModeNv = gl.Constants.GL_PATH_FILL_COVER_MODE_NV, 
#endif

#if GL_NV_PATH_RENDERING
    StrokeCoverModeNv = gl.Constants.GL_PATH_STROKE_COVER_MODE_NV, 
#endif

#if GL_NV_PATH_RENDERING
    StrokeMaskNv = gl.Constants.GL_PATH_STROKE_MASK_NV, 
#endif

#if GL_NV_PATH_RENDERING
    ObjectBoundingBoxNv = gl.Constants.GL_PATH_OBJECT_BOUNDING_BOX_NV, 
#endif

#if GL_NV_PATH_RENDERING
    CommandCountNv = gl.Constants.GL_PATH_COMMAND_COUNT_NV, 
#endif

#if GL_NV_PATH_RENDERING
    CoordCountNv = gl.Constants.GL_PATH_COORD_COUNT_NV, 
#endif

#if GL_NV_PATH_RENDERING
    DashArrayCountNv = gl.Constants.GL_PATH_DASH_ARRAY_COUNT_NV, 
#endif

#if GL_NV_PATH_RENDERING
    ComputedLengthNv = gl.Constants.GL_PATH_COMPUTED_LENGTH_NV, 
#endif

#if GL_NV_PATH_RENDERING
    FillBoundingBoxNv = gl.Constants.GL_PATH_FILL_BOUNDING_BOX_NV, 
#endif

#if GL_NV_PATH_RENDERING
    StrokeBoundingBoxNv = gl.Constants.GL_PATH_STROKE_BOUNDING_BOX_NV, 
#endif

#if GL_NV_PATH_RENDERING
    DashOffsetResetNv = gl.Constants.GL_PATH_DASH_OFFSET_RESET_NV, 
#endif

}

#endif
