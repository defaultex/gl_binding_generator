#if GL_NV_path_rendering

public enum PathParameter : GLenum {
#if GL_NV_path_rendering
    PathStrokeWidthNv = gl.Constants.GL_PATH_STROKE_WIDTH_NV,
#endif

#if GL_NV_path_rendering
    PathEndCapsNv = gl.Constants.GL_PATH_END_CAPS_NV,
#endif

#if GL_NV_path_rendering
    PathInitialEndCapNv = gl.Constants.GL_PATH_INITIAL_END_CAP_NV,
#endif

#if GL_NV_path_rendering
    PathTerminalEndCapNv = gl.Constants.GL_PATH_TERMINAL_END_CAP_NV,
#endif

#if GL_NV_path_rendering
    PathJoinStyleNv = gl.Constants.GL_PATH_JOIN_STYLE_NV,
#endif

#if GL_NV_path_rendering
    PathMiterLimitNv = gl.Constants.GL_PATH_MITER_LIMIT_NV,
#endif

#if GL_NV_path_rendering
    PathDashCapsNv = gl.Constants.GL_PATH_DASH_CAPS_NV,
#endif

#if GL_NV_path_rendering
    PathInitialDashCapNv = gl.Constants.GL_PATH_INITIAL_DASH_CAP_NV,
#endif

#if GL_NV_path_rendering
    PathTerminalDashCapNv = gl.Constants.GL_PATH_TERMINAL_DASH_CAP_NV,
#endif

#if GL_NV_path_rendering
    PathDashOffsetNv = gl.Constants.GL_PATH_DASH_OFFSET_NV,
#endif

#if GL_NV_path_rendering
    PathClientLengthNv = gl.Constants.GL_PATH_CLIENT_LENGTH_NV,
#endif

#if GL_NV_path_rendering
    PathFillModeNv = gl.Constants.GL_PATH_FILL_MODE_NV,
#endif

#if GL_NV_path_rendering
    PathFillMaskNv = gl.Constants.GL_PATH_FILL_MASK_NV,
#endif

#if GL_NV_path_rendering
    PathFillCoverModeNv = gl.Constants.GL_PATH_FILL_COVER_MODE_NV,
#endif

#if GL_NV_path_rendering
    PathStrokeCoverModeNv = gl.Constants.GL_PATH_STROKE_COVER_MODE_NV,
#endif

#if GL_NV_path_rendering
    PathStrokeMaskNv = gl.Constants.GL_PATH_STROKE_MASK_NV,
#endif

#if GL_NV_path_rendering
    PathObjectBoundingBoxNv = gl.Constants.GL_PATH_OBJECT_BOUNDING_BOX_NV,
#endif

#if GL_NV_path_rendering
    PathCommandCountNv = gl.Constants.GL_PATH_COMMAND_COUNT_NV,
#endif

#if GL_NV_path_rendering
    PathCoordCountNv = gl.Constants.GL_PATH_COORD_COUNT_NV,
#endif

#if GL_NV_path_rendering
    PathDashArrayCountNv = gl.Constants.GL_PATH_DASH_ARRAY_COUNT_NV,
#endif

#if GL_NV_path_rendering
    PathComputedLengthNv = gl.Constants.GL_PATH_COMPUTED_LENGTH_NV,
#endif

#if GL_NV_path_rendering
    PathFillBoundingBoxNv = gl.Constants.GL_PATH_FILL_BOUNDING_BOX_NV,
#endif

#if GL_NV_path_rendering
    PathStrokeBoundingBoxNv = gl.Constants.GL_PATH_STROKE_BOUNDING_BOX_NV,
#endif

#if GL_NV_path_rendering
    PathDashOffsetResetNv = gl.Constants.GL_PATH_DASH_OFFSET_RESET_NV,
#endif
}

#endif